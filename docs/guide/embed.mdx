---
title: Inline Canvas Embed
---

# Inline Canvas Embed

You can embed a canvas directly into any MDX page.

<CanvasEmbed src="Demo.canvas" />

Rspress base paths are detected automatically. If the site is served behind a proxy that does not expose its base in asset URLs, pass it explicitly:

```mdx
<CanvasEmbed src="Demo.canvas" basePath="/docs" />
```

The embed supports the same features as the full canvas page: pan, zoom, edge highlighting, and all node types.
